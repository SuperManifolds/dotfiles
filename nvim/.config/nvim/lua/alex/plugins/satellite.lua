return {
    {
        "lewis6991/satellite.nvim",
        event = "BufReadPost",
        opts = {
            -- Decorated scrollbar with position marks. Default handlers cover
            -- cursor, search, diagnostics, gitsigns, marks and quickfix — the
            -- functionality previously provided by nvim-scrollbar.
        },
    },
}
